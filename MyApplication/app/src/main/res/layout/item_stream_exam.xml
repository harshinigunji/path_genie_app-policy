<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_card_white"
    android:padding="16dp"
    android:layout_marginBottom="12dp">

    <!-- Conducting Body -->
    <TextView
        android:id="@+id/conductingBody"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="MOE (GOVT. OF INDIA)"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="#2563EB" />

    <!-- Row with Exam Name and Arrow -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="4dp">

        <TextView
            android:id="@+id/examName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="NMMS"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#111827" />

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_arrow_right"
            android:tint="#2563EB" />
    </LinearLayout>

    <!-- Overview/Description -->
    <TextView
        android:id="@+id/examOverview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="National Means-cum-Merit Scholarship Scheme for meritorious students from EWS."
        android:textSize="14sp"
        android:textColor="#6B7280"
        android:lineSpacingMultiplier="1.3"
        android:layout_marginTop="8dp" />

    <!-- Category Tag -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_chip_light"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:layout_marginTop="12dp">

        <ImageView
            android:id="@+id/categoryIcon"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:src="@drawable/ic_cap" />

        <TextView
            android:id="@+id/categoryTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Scholarship Exam"
            android:textSize="12sp"
            android:textColor="#374151"
            android:layout_marginStart="6dp" />
    </LinearLayout>

</LinearLayout>
